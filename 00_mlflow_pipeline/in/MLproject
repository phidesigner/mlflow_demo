name: input_step
conda_env: conda.yml

entry_points:
  main:
    parameters:
      text:
        description: Text to print
        type: str
        default: default text
    command: >-
      python run_in.py --text {text}
